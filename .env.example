# The single root .env.example has been split per service:
#
#   backend/.env.example   -> copy to backend/.env        (FastAPI settings)
#   frontend/.env.example  -> copy to frontend/.env.local (Next.js settings)
#
# Each service only reads its own file. See README.md for details.
